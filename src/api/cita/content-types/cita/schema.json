{
  "kind": "collectionType",
  "collectionName": "citas",
  "info": {
    "singularName": "cita",
    "pluralName": "citas",
    "displayName": "cita",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "estado": {
      "type": "enumeration",
      "enum": [
        "pendiente",
        "confirmar",
        "cancelar"
      ],
      "default": "pendiente",
      "required": true
    },
    "barbero": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::barbero.barbero"
    }
  }
}
